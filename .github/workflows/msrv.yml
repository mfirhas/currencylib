name: msrv

env:
  RUST_VERSION: 1.95.0

on:
  workflow_dispatch: {}

concurrency:
  group: currencylib-ci
  cancel-in-progress: true

jobs:
  msrv:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v6

      - name: Cache run data
        id: ci-cache-msrv
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.cargo/bin
            target
          key: ${{ runner.os }}-ci-cache-msrv-${{ env.RUST_VERSION }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-ci-cache-

      - name: Install stable toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: ${{ env.RUST_VERSION }}
          override: true

      - name: Install cargo-msrv
        run: which cargo-msrv || cargo install cargo-msrv

      - name: Run cargo msrv
        uses: actions-rs/cargo@v1
        with:
          command: msrv
          args: verify --all-features
